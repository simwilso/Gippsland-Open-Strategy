{
  "name": "gippsland-intake-system",
  "version": "1.0.0",
  "description": "Intake system for processing community suggestions",
  "type": "module",
  "scripts": {
    "map": "tsx mapper.ts",
    "validate": "tsx validate.ts",
    "test": "npm run test:mapper && npm run test:validator",
    "test:mapper": "tsx mapper.ts test-issue.json | tsx validate.ts /dev/stdin",
    "test:validator": "tsx mapper.ts test-issue.json test-output.json && tsx validate.ts test-output.json"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}